<?xml version="1.0" encoding="UTF-8"?>
<user>
    {% if user %}
    {% with user.get_profile as profile %}
    <avatar>{% if profile.avatar %}{{ profile.avatar.url }}{% endif %}</avatar>
    <country>{{ profile.country }}</country>
    <group>{{ profile.group.id }}</group>
    <infoline>{{ profile.infoline }}</infoline>
    <location>{{ profile.location }}</location>
    <realname>{{ profile.real_name }}</realname>
    <twitterid>{{ profile.twitter_id }}</twitterid>
    <webpage>{{ profile.web_page }}</webpage>
    <favorites>
    {% for fav in user.favorite_set.all %}<song id="{{ fav.song.id }}" locked="{{ fav.song.is_locked }}">{{ fav.song.title }}</song>{% endfor %}
    </favorites>
    {% endwith %}
    {% else %}
    <hidden/>
    {% endif %}
</user>
